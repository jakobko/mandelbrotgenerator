from setuptools import setup

setup(name='mandelbrot',
      version='1',
      description='A mandelbrot generator',
      url='http://github.com/jakobko',
      author='jakobko',
      author_email='jakobko@ifi.uio.no',
      packages=['mandelbrot'],
      zip_safe=False)
